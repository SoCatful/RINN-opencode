## 修改计划：将Y维度改为101维（dB值）训练

### 需要修改的文件：
1. **trains11RINN.py** - 主要训练脚本

### 修改内容：

#### 1. 修改数据加载函数 (`load_data_from_csv`)
- **当前实现**：提取实部和虚部数据，合并为202维输出
- **修改方案**：计算S11 dB值，使用公式 20 	imes og_{10}(qrt{	ext{real}^2 + 	ext{imag}^2})，输出101维dB值

#### 2. 修改数据归一化部分
- **当前实现**：对202维数据进行归一化，包含硬编码的202维限制
- **修改方案**：移除硬编码限制，使用动态维度，适应101维dB值

#### 3. 修改维度处理部分
- **当前实现**：设置Y维度为202维
- **修改方案**：更新Y维度为101维，相应调整padding维度和输入维度

#### 4. 保持其他参数不变
- 模型配置参数（hidden_dim, num_blocks等）
- 训练参数（batch_size, learning_rate等）
- 损失权重（weight_y, weight_x, weight_z）

### 预期结果：
- 模型使用101维dB值进行训练
- 输入维度从207维减少到106维（5+101）
- 其他参数保持不变，确保训练过程一致
- 能够成功训练模型并观察性能