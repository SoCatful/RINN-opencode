## 修改计划：使bayesian_optimization.py与trains11RINN.py一致

### 需要修改的文件：
1. **bayesian_optimization.py** - 贝叶斯优化脚本

### 修改内容：

#### 1. 修改数据加载函数 (`load_data_from_csv`)
- **当前实现**：提取实部和虚部数据，合并为202维输出
- **修改方案**：计算S11 dB值，使用公式 20 	imes og_{10}(qrt{	ext{real}^2 + 	ext{imag}^2})，输出101维dB值

#### 2. 修改数据归一化函数 (`normalize_data`)
- **当前实现**：对202维数据进行归一化，包含硬编码的202维限制
- **修改方案**：移除硬编码限制，使用动态维度，适应101维dB值

#### 3. 修改维度处理部分
- **当前实现**：设置Y维度为202维，输入维度为207维
- **修改方案**：更新Y维度为101维，相应调整padding维度和输入维度为106维

#### 4. 保持训练逻辑一致
- 确保训练函数使用与trains11RINN.py相同的Z采样策略
- 保持损失计算方法一致
- 保持早停和学习率调度器设置一致

### 预期结果：
- bayesian_optimization.py与trains11RINN.py使用相同的dB值训练方法
- 两个文件的数据处理和模型配置完全一致
- 贝叶斯优化能够找到适合dB值训练的最佳超参数
- 确保低耦合性，便于后续维护和更新